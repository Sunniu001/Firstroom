{%- comment -%}
  FR Custom FAQ Section
{%- endcomment -%}

<section id="fr-custom-faq-{{ section.id }}" class="fr-custom-faq" style="
  background-color: {{ section.settings.bg_color }};
  padding-top: {{ section.settings.padding_top_mobile | default: section.settings.padding_top }}px;
  padding-bottom: {{ section.settings.padding_bottom_mobile | default: section.settings.padding_bottom }}px;
  --pt-d: {{ section.settings.padding_top }}px;
  --pb-d: {{ section.settings.padding_bottom }}px;
  padding-left: {{ section.settings.padding_left }}px;
  padding-right: {{ section.settings.padding_right }}px;
">
  <div class="fr-custom-faq__container">
    {%- if section.settings.heading != blank -%}
      <h2 class="fr-custom-faq__heading" style="
        --fz-hd-d: {{ section.settings.heading_size }}px;
        --fz-hd-m: {{ section.settings.heading_size_mobile | default: section.settings.heading_size | times: 0.8 | round }}px;
        color: {{ section.settings.heading_color }};
        --ha-d: {{ section.settings.heading_alignment | default: 'center' }};
        --ha-m: {{ section.settings.heading_align_mobile | default: section.settings.heading_alignment | default: 'center' }};
      ">{{ section.settings.heading }}</h2>
    {%- endif -%}

    <div class="fr-custom-faq__list">
      {%- for block in section.blocks -%}
        <div class="fr-custom-faq__item" {{ block.shopify_attributes }}>
          <button class="fr-custom-faq__question" aria-expanded="false" aria-controls="faq-answer-{{ block.id }}">
            <span class="fr-custom-faq__question-text">{{ block.settings.question }}</span>
            <span class="fr-custom-faq__icon">+</span>
          </button>
          <div id="faq-answer-{{ block.id }}" class="fr-custom-faq__answer">
            <div class="fr-custom-faq__answer-inner">
              {{ block.settings.answer }}
            </div>
          </div>
        </div>
      {%- endfor -%}
    </div>
  </div>
</section>

<style>
  #fr-custom-faq-{{ section.id }} .fr-custom-faq__container {
    max-width: 800px;
    margin: 0 auto;
  }

  #fr-custom-faq-{{ section.id }} .fr-custom-faq__heading {
    margin: 0 0 40px 0;
    text-align: var(--ha-m);
    font-size: var(--fz-hd-m) !important;
    font-family: 'Playfair Display', Georgia, serif;
    font-weight: 500;
  }
  
  @media (min-width: 768px) {
    #fr-custom-faq-{{ section.id }} {
      padding-top: var(--pt-d) !important;
      padding-bottom: var(--pb-d) !important;
    }
    #fr-custom-faq-{{ section.id }} .fr-custom-faq__heading {
      text-align: var(--ha-d);
      font-size: var(--fz-hd-d) !important;
    }
  }

  #fr-custom-faq-{{ section.id }} .fr-custom-faq__list {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  #fr-custom-faq-{{ section.id }} .fr-custom-faq__item {
    background-color: #f5f5f5;
  }

  #fr-custom-faq-{{ section.id }} .fr-custom-faq__question {
    width: 100%;
    text-align: left;
    padding: 18px 24px;
    background: none;
    border: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
    cursor: pointer;
    font-family: 'Lato', system-ui, sans-serif;
    font-size: 16px;
    color: #1a1a1a;
    font-weight: 600;
  }

  #fr-custom-faq-{{ section.id }} .fr-custom-faq__icon {
    font-size: 20px;
    font-weight: 400;
    transition: transform 0.3s ease;
  }

  #fr-custom-faq-{{ section.id }} .fr-custom-faq__question[aria-expanded="true"] .fr-custom-faq__icon {
    transform: rotate(45deg);
  }

  #fr-custom-faq-{{ section.id }} .fr-custom-faq__answer {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease;
  }

  #fr-custom-faq-{{ section.id }} .fr-custom-faq__answer-inner {
    padding: 0 24px 24px 24px;
    font-family: 'Lato', system-ui, sans-serif;
    font-size: 14px;
    color: #444;
    line-height: 1.5;
  }

  #fr-custom-faq-{{ section.id }} .fr-custom-faq__answer-inner p:first-child { margin-top: 0; }
  #fr-custom-faq-{{ section.id }} .fr-custom-faq__answer-inner p:last-child { margin-bottom: 0; }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const faqSection = document.getElementById('fr-custom-faq-{{ section.id }}');
    if (!faqSection) return;

    const buttons = faqSection.querySelectorAll('.fr-custom-faq__question');

    buttons.forEach(button => {
      button.addEventListener('click', function() {
        const isExpanded = this.getAttribute('aria-expanded') === 'true';
        const answer = this.nextElementSibling;
        
        // Close all others
        buttons.forEach(btn => {
          if (btn !== this) {
            btn.setAttribute('aria-expanded', 'false');
            btn.nextElementSibling.style.maxHeight = null;
          }
        });

        // Toggle current
        if (isExpanded) {
          this.setAttribute('aria-expanded', 'false');
          answer.style.maxHeight = null;
        } else {
          this.setAttribute('aria-expanded', 'true');
          answer.style.maxHeight = answer.scrollHeight + "px";
        }
      });
    });
  });
</script>

{% schema %}
{
  "name": "FR Custom FAQ",
  "tag": "section",
  "settings": [
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#ffffff"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "FAQ's"
    },
    {
      "type": "range",
      "id": "heading_size",
      "min": 20,
      "max": 60,
      "step": 2,
      "unit": "px",
      "label": "Heading Size (Desktop)",
      "default": 30
    },
    {
      "type": "range",
      "id": "heading_size_mobile",
      "min": 16,
      "max": 60,
      "step": 1,
      "unit": "px",
      "label": "Heading Size (Mobile)",
      "default": 24
    },
    {
      "type": "select",
      "id": "heading_alignment",
      "label": "Heading Alignment (Desktop)",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" },
        { "value": "right", "label": "Right" }
      ],
      "default": "center"
    },
    {
      "type": "select",
      "id": "heading_align_mobile",
      "label": "Heading Alignment (Mobile)",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" },
        { "value": "right", "label": "Right" }
      ],
      "default": "center"
    },
    {
      "type": "color",
      "id": "heading_color",
      "label": "Heading Color",
      "default": "#1a1a1a"
    },
    {
      "type": "header",
      "content": "Spacing"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "Padding Top",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "Padding Bottom (Desktop)",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_top_mobile",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "Padding Top (Mobile)",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding_bottom_mobile",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "Padding Bottom (Mobile)",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding_left",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "Padding Left",
      "default": 20
    },
    {
      "type": "range",
      "id": "padding_right",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "Padding Right",
      "default": 20
    }
  ],
  "blocks": [
    {
      "type": "faq",
      "name": "FAQ Item",
      "settings": [
        {
          "type": "text",
          "id": "question",
          "label": "Question",
          "default": "Frequently asked question?"
        },
        {
          "type": "richtext",
          "id": "answer",
          "label": "Answer",
          "default": "<p>Here is the answer to the frequently asked question.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "FR Custom FAQ",
      "blocks": [
        { "type": "faq" },
        { "type": "faq" },
        { "type": "faq" },
        { "type": "faq" }
      ]
    }
  ]
}
{% endschema %}
